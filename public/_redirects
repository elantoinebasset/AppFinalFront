# Proxy des appels API vers le backend Render (evite les problemes de CORS : le navigateur ne voit que le domaine Netlify)
/api/*  https://appfinalback.onrender.com/api/:splat  200

# Fallback SPA : toutes les autres routes renvoient index.html (routing cote client Vue)
/*      /index.html  200
